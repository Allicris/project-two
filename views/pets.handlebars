<section class="container">
  <h3 class="pets-name">{{pets.name}}</h3>
  {{!-- <h4>Opening Hour: {{format_time gallery.starting_date}}</h4>
  <h4>Closing Hour: {{format_time gallery.ending_date}}</h4> --}}
  
  {{!-- We should have pets.cats and pets.dogs? --}}
  {{#if loggedIn}}
  <section class="flex-row justify-center">
    {{#each gallery.paintings as |painting|}}
    {{!-- cats and dogs details? --}}
    {{> painting-details}}
    {{/each}}
  </section>
</section>
{{else}}
<a href="/login">You must login first to view this page</a>
{{/if}}