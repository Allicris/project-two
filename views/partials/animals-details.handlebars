<section class="card">
  <a href="/animals/{{id}}">
    <header>{{name}}</header>

    <div class="card-body">
      <img src="/images{{filename}}" alt="{{breed}}">
    <p>Don't shop, ADOPT! <i class="fa fa-heart" aria-hidden="true"></i></p>
    
    </div>
  </a>
</section>