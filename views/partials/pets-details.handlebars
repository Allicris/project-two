<section class="card">
  <a href="/pets/{{id}}">
    <header>{{name}}</header>
    <img src="/images{{animals.[0].filename}}" alt="{{animals.[0].name}}" width="300px" style="display:flex">
  </a>
</section>